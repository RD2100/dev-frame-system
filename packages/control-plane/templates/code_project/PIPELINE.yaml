# Pipeline definition generated by DevFrame Control Plane.

pipeline_id: code_project
version: 1
description: "Starter evidence-first software project pipeline"

global_invariants:
  guard_removal_approved: false
  evidence_cleanup_approved: false

stages:
  - id: g0_bootstrap
    type: bootstrap
    description: "Initialize project context and safety boundaries"
    requires_gpt_acceptance: false

  - id: g1_pipeline_definition
    type: planning
    description: "Define the next bounded task, evidence, and review criteria"
    depends_on: g0_bootstrap
    requires_gpt_acceptance: false

  - id: g2_authorization
    type: authorization
    description: "Request external-brain approval before risky implementation"
    depends_on: g1_pipeline_definition
    requires_gpt_acceptance: true
    evidence_spec:
      required_files:
        - NEXT_TASK.md
        - SAFETY_BOUNDARIES.md

  - id: g3_execution_closure
    type: execution_closure
    description: "Collect implementation evidence and reviewer-facing report"
    depends_on: g2_authorization
    requires_gpt_acceptance: true
    evidence_spec:
      required_files:
        - EVIDENCE_SPEC.yaml
        - CURRENT_STATE.yaml
