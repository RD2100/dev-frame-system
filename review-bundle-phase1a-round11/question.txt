Round: 11
Commit: 6325415

## 变更摘要

修复 Round 10 PASS 中提到的 2 个 P2 建议：

1. derive_projection 状态推导改用优先级逻辑
   - 之前：反向迭代 + break，交错顺序可能返回错误状态
   - 现在：按优先级扫描所有决策（gate pass > human_required > blocked > insufficient_evidence > review pass > ready）

2. insufficient_evidence allowed_actions 与 fixture 对齐
   - 之前：[view, add_evidence, review]
   - 现在：[view, escalate, gather_more_evidence]（与 insufficient-evidence.json fixture 一致）

3. 新增交错顺序测试
   - test_derive_projection_interleaved_decisions_preserves_order
   - 验证 gate 在前、review 在后时，latest_decision_id 保持原始顺序，computed_status 仍为 completed

## 统计

- 68 个测试全部通过

## 审查问题

1. derive_projection 是否已修复交错顺序问题？
2. allowed_actions 是否与 fixture 对齐？
3. 68 个测试是否全部通过？

请返回 PASS / CONDITIONAL PASS / NO-GO。
