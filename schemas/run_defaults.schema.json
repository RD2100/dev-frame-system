{
  "title": "Run Defaults",
  "description": "Project-level visual config for default run parameters (worker count, model, default methodology). Stored at <runtime>/run-defaults.json (global) and <runtime>/{project_id}/run-defaults.json (project). Each field inherits independently across built-in < global < project; an absent field inherits the next-less-specific scope.",
  "type": "object",
  "required": ["version"],
  "properties": {
    "version": { "type": "integer", "minimum": 1 },
    "agents": { "type": "integer", "minimum": 1 },
    "model": { "type": "string", "minLength": 1 },
    "methodology": { "type": "string", "minLength": 1 }
  },
  "additionalProperties": false
}
