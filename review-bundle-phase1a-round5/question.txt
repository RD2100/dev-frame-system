请重新审查 Round 4 修复版本。

上轮 NO_GO 的 P0-1 修复：gate decision 的 target_ref 必须匹配 work_item.id。
- validate_packet() 现在检查 Decision(kind=gate, outcome=pass, target_ref=work_item.id)
- 新增测试 test_completed_requires_gate_for_correct_work_item: gate 指向其他 work item 时被拒绝

测试: 45 passed

请验证 target_ref 约束是否封住。recommendation: GO / NO_GO