Round: 10
Commit: bcc43bb

## 变更摘要

修复 Round 9 NO-GO 的 4 个问题：

1. missing-context 场景：无 decisions 时 derive_projection 不再推导为 ready，而是检查 work_item.status，若为 blocked 则保持 blocked
2. blocked_reason：优先从 gate decision 的 payload.blocked_reasons 提取，使用 _extract_blocked_reason() 辅助函数
3. latest_decision_id：使用原始 decisions 数组顺序（按 target_ref 过滤后取最后一个），不再按 kind 拼接
4. 状态推导：使用反向迭代（reversed），最新有效决策优先，历史 blocked 可被后续 gate pass 覆盖

## 新增测试

- test_derive_projection_missing_context_is_blocked
- test_derive_projection_blocked_reason_from_gate_payload
- test_derive_projection_latest_decision_preserves_original_order

## 统计

- 67 个测试全部通过

## 审查问题

1. missing-context 场景是否现在正确推导为 blocked？
2. blocked_reason 是否从 gate payload 正确提取？
3. latest_decision_id 是否保持原始 decisions 顺序？
4. 67 个测试是否全部通过？

请返回 PASS / CONDITIONAL PASS / NO-GO。
